# DASH Desktop

Electron + React + TypeScript desktop application for DASH.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The build compiles shared packages first, then bundles the renderer and Electron main process via Vite.
